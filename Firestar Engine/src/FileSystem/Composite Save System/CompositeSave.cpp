#include "FileSystem/Composite Save System/CompositeSave.h"

std::vector<unsigned char> alldata;

void CompositeSave::save(std::string saveLocation) {
    // File Format
    // Count of Data

    for(IComposite i : data) {
        
    }
}